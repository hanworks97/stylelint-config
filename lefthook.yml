# https://lefthook.dev/configuration/

pre-commit:
  commands:
    eslint:
      glob: '*'
      run: pnpm eslint --no-warn-ignored --cache --fix {staged_files}

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit $1
